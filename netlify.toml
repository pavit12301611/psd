[build]
  functions = "functions"
  publish = "."

[dev]
  command = "echo 'Static site'"
  port = 8888
  targetPort = 8888

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200

[functions]
  node_bundler = "esbuild"